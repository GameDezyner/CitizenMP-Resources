==Usage
```LUA
exports.notificationmanager:showNotification("Message Text") --Shows a notification in the bottom left. Multiple notifications with the same message will not pop up, it will just stay up for longer.
```